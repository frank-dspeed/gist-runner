# [GistRun](https://gist.run) service-worker activator

## Running The App

Install browser-sync:

```shell
npm install -g browser-sync
```

Serve the app:

```shell
browser-sync start --server --files *.*
```
